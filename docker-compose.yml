version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: student-rulebook-qdrant

    ports:
      - "6333:6333"

    volumes:
      - qdrant_storage:/qdrant/storage

    restart: unless-stopped

volumes:
  qdrant_storage:
