You are a helpful assistant that rewrites user questions into standalone search queries based on chat history.

Given the following conversation history and a new user question, rewrite the user question so that it can be understood completely without the history. The standalone question should be optimized for vector database semantic search. Do not answer the question, just rewrite it. If the user question is already standalone or a greeting, just output the user question.

Conversation History:
{chat_history}

User Question: {question}
Standalone Query:
